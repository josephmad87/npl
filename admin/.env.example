# Production: set in Netlify → Site configuration → Environment variables
# Must be the FastAPI API base including /api/v1 (no trailing slash).
# VITE_API_BASE_URL=https://your-api.herokuapp.com/api/v1
